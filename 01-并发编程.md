# 1. 并发编程

## 1.1 Java 内存模型 (JMM)

Java内存模型定义了线程与主内存之间的抽象关系，确保多线程程序的可见性、有序性和原子性。

### 1.1.1 三大特性

| 特性 | 说明 | 保障机制 |
|------|------|----------|
| **原子性** | 操作不可被中断 | `synchronized`、`Lock`、CAS、原子类 |
| **可见性** | 一个线程修改对其他线程立即可见 | `volatile`、`synchronized`、`final` |
| **有序性** | 程序执行顺序与代码顺序一致 | `volatile`、`synchronized`、内存屏障 |

### 1.1.2 happens-before 原则

JMM通过happens-before规则保证可见性，核心规则包括：

- **程序顺序规则**：同一线程中，前面的操作happens-before后面的操作
- **锁规则**：锁的`unlock`操作happens-before后续对同一锁的`lock`操作
- **volatile规则**：volatile写happens-before后续对该变量的读
- **线程启动规则**：`Thread.start()` happens-before该线程的每个动作
- **线程终止规则**：线程的所有操作都happens-before `Thread.join()` 返回
- **传递性**：如果A happens-before B，B happens-before C，则A happens-before C

```java
// volatile 保证可见性和有序性，但不保证复合操作的原子性
public class Counter {
    private volatile int count = 0;
    
    // 非原子操作：读取count → 加1 → 写回count
    public void increment() {
        count++; // 不是线程安全的
    }
    
    // 使用原子类保证原子性
    private AtomicInteger atomicCount = new AtomicInteger(0);
    
    public void safeIncrement() {
        atomicCount.incrementAndGet(); // CAS 保证原子性
    }
}
```

### 1.1.3 volatile 的实现原理

volatile通过**内存屏障**（Memory Barrier）实现可见性和有序性：

- **写操作**：在volatile写之后插入`Store-Barrier`，强制将缓存中的数据刷回主内存
- **读操作**：在volatile读之前插入`Load-Barrier`，强制从主内存重新加载数据
- **禁止指令重排序**：内存屏障充当指令重排的边界

---

## 1.2 synchronized 与 Lock

### 1.2.1 synchronized 关键字

`synchronized`基于**对象监视器**（Monitor）实现，每个对象关联一个monitor：

```java
// 同步实例方法：锁是this对象
public synchronized void instanceMethod() { }

// 同步静态方法：锁是Class对象
public static synchronized void staticMethod() { }

// 同步代码块：锁是指定对象
public void blockMethod() {
    synchronized (lockObject) { }
}
```

**synchronized 的升级过程**（JDK 1.6+）：

1. **无锁** → **偏向锁**：首个线程进入时，将Mark Word中的线程ID设为偏向线程
2. **偏向锁** → **轻量级锁**：有第二个线程竞争，升级为轻量级锁（CAS自旋）
3. **轻量级锁** → **重量级锁**：自旋超过阈值或有第三个线程竞争，升级为重量级锁（操作系统互斥量）

```java
// synchronized 是可重入锁
public class ReentrantExample {
    public synchronized void methodA() {
        methodB(); // 同一线程可再次获取该锁
    }
    
    public synchronized void methodB() {
        System.out.println("可重入获取锁成功");
    }
}
```

### 1.2.2 Lock 接口

`ReentrantLock`提供了比`synchronized`更灵活的锁机制：

```java
public class LockExample {
    private final ReentrantLock lock = new ReentrantLock();
    
    public void safeMethod() {
        lock.lock();
        try {
            // 临界区代码
        } finally {
            lock.unlock(); // 必须在finally中释放
        }
    }
    
    // 尝试获取锁，支持超时和中断
    public boolean tryLockMethod() throws InterruptedException {
        if (lock.tryLock(1, TimeUnit.SECONDS)) {
            try {
                return true;
            } finally {
                lock.unlock();
            }
        }
        return false;
    }
}
```

**synchronized vs ReentrantLock**：

| 特性 | synchronized | ReentrantLock |
|------|-------------|---------------|
| 实现 | JVM层面 | API层面（JDK） |
| 释放 | 自动释放 | 需手动unlock |
| 可中断 | 不支持 | 支持`lockInterruptibly()` |
| 超时 | 不支持 | 支持`tryLock(timeout)` |
| 公平锁 | 不支持 | 支持公平/非公平模式 |
| 条件变量 | 单条件（wait/notify） | 多条件（Condition） |

---

## 1.3 CAS 与原子类

### 1.3.1 CAS 原理

CAS（Compare And Swap）是一种无锁原子操作，CPU级别支持（`cmpxchg`指令）：

```java
// AtomicInteger.incrementAndGet() 的简化实现
public final int incrementAndGet() {
    int oldValue;
    int newValue;
    do {
        oldValue = get(); // 读取当前值
        newValue = oldValue + 1; // 计算新值
    } while (!compareAndSet(oldValue, newValue)); // CAS：比较并交换
    return newValue;
}
```

### 1.3.2 原子类

`java.util.concurrent.atomic`包提供了多种原子类：

| 类 | 说明 |
|----|------|
| `AtomicInteger` / `AtomicLong` | 原子整型/长整型 |
| `AtomicBoolean` | 原子布尔型 |
| `AtomicReference` | 原子引用 |
| `AtomicStampedReference` | 带时间戳的原子引用（解决ABA问题） |
| `AtomicIntegerArray` / `AtomicLongArray` | 原子数组 |
| `LongAdder` / `DoubleAdder` | 高并发累加器（分段CAS） |

### 1.3.3 ABA 问题

CAS存在ABA问题：值从A→B→A，CAS认为没有变化。解决方案：

```java
// 使用 AtomicStampedReference 解决ABA问题
AtomicStampedReference<Integer> stampedRef = 
    new AtomicStampedReference<>(100, 0);

int stamp = stampedRef.getStamp();
Integer reference = stampedRef.getReference();

// CAS时同时检查值和时间戳
stampedRef.compareAndSet(reference, 200, stamp, stamp + 1);
```

---

## 1.4 AQS 原理

AQS（AbstractQueuedSynchronizer）是JUC包的核心框架，实现了锁、信号量、倒计时闩等同步工具。

### 1.4.1 核心设计

```java
public abstract class AbstractQueuedSynchronizer {
    // state 字段：表示同步状态
    // - 独占模式：0表示未锁定，>0表示锁定（可重入次数）
    // - 共享模式：表示剩余许可数
    private volatile int state;
    
    // CLH双向队列：存储等待获取锁的线程
    private transient Node head;
    private transient Node tail;
}
```

### 1.4.2 同步分类

- **独占式**：同一时刻只允许一个线程获取（如`ReentrantLock`、`ReentrantReadWriteLock.WriteLock`）
- **共享式**：允许多个线程同时获取（如`Semaphore`、`CountDownLatch`、`ReentrantReadWriteLock.ReadLock`）

### 1.4.3 基于AQS的同步组件

```java
// ReentrantLock：独占式
ReentrantLock lock = new ReentrantLock();

// Semaphore：共享式，控制并发线程数
Semaphore semaphore = new Semaphore(3); // 最多3个许可
semaphore.acquire();
try {
    // 临界区
} finally {
    semaphore.release();
}

// CountDownLatch：等待多个任务完成
CountDownLatch latch = new CountDownLatch(5);
for (int i = 0; i < 5; i++) {
    new Thread(() -> {
        try {
            // 执行任务
        } finally {
            latch.countDown();
        }
    }).start();
}
latch.await(); // 等待所有任务完成

// CyclicBarrier：循环屏障
CyclicBarrier barrier = new CyclicBarrier(3, () -> {
    System.out.println("所有线程到达屏障，继续执行");
});
// 每个线程在关键点调用 barrier.await()
```

---

## 1.5 并发容器

### 1.5.1 线程安全集合

| 集合 | 实现方式 | 说明 |
|------|----------|------|
| `ConcurrentHashMap` | CAS + synchronized分段锁 | JDK1.8：CAS+synchronized锁单个Node |
| `CopyOnWriteArrayList` | 写时复制 | 读多写少场景 |
| `BlockingQueue` | 锁+条件队列 | 生产者-消费者模型 |
| `ConcurrentSkipListMap` | 跳表+CAS | 有序并发Map |

### 1.5.2 ConcurrentHashMap 原理（JDK 1.8）

```java
// 数据结构：Node数组 + 链表 + 红黑树
transient Node<K,V>[] table;

// put 核心流程
final V putVal(K key, V value, boolean onlyIfAbsent) {
    for (Node<K,V>[] tab = table;;) {
        Node<K,V> f; int n, i, hash;
        
        if (tab == null) tab = initTable(); // 初始化
        else if ((f = tabAt(tab, i = (n-1) & hash)) == null) {
            // CAS 写入空桶
            if (casTabAt(tab, i, null, new Node<>(hash, key, value)))
                break;
        } else if ((fh = f.hash) == MOVED)
            tab = helpTransfer(tab, f); // 协助扩容
        else {
            // synchronized 锁定链表头节点
            synchronized (f) {
                // 遍历链表/红黑树，查找或插入
            }
        }
    }
}
```

### 1.5.3 BlockingQueue 体系

```java
// ArrayBlockingQueue：基于数组的有界阻塞队列
ArrayBlockingQueue<String> arrayQueue = new ArrayBlockingQueue<>(10);

// LinkedBlockingQueue：基于链表的有界/无界阻塞队列
LinkedBlockingQueue<String> linkedQueue = new LinkedBlockingQueue<>();

// PriorityBlockingQueue：优先级阻塞队列
PriorityBlockingQueue<Task> priorityQueue = new PriorityBlockingQueue<>();

// DelayQueue：延迟队列（定时任务场景）
DelayQueue<ScheduledTask> delayQueue = new DelayQueue<>();

// SynchronousQueue：直接交换（无缓冲）
SynchronousQueue<String> synchronousQueue = new SynchronousQueue<>();
```

---

## 1.6 线程池

### 1.6.1 ThreadPoolExecutor 核心参数

```java
public ThreadPoolExecutor(
    int corePoolSize,        // 核心线程数（常驻）
    int maximumPoolSize,     // 最大线程数
    long keepAliveTime,      // 非核心线程空闲存活时间
    TimeUnit unit,           // 存活时间单位
    BlockingQueue<Runnable> workQueue,  // 任务队列
    ThreadFactory threadFactory,        // 线程工厂
    RejectedExecutionHandler handler    // 拒绝策略
)
```

### 1.6.2 任务提交流程

```
提交任务
  ↓
核心线程数未满？ → 是 → 创建核心线程执行
  ↓ 否
队列未满？ → 是 → 入队等待
  ↓ 否
最大线程数未满？ → 是 → 创建非核心线程执行
  ↓ 否
执行拒绝策略
```

### 1.6.3 四种拒绝策略

| 策略 | 说明 |
|------|------|
| `AbortPolicy` | 抛出异常（默认） |
| `CallerRunsPolicy` | 调用者线程执行 |
| `DiscardPolicy` | 静默丢弃 |
| `DiscardOldestPolicy` | 丢弃最旧任务 |

### 1.6.4 自定义线程池

```java
public class ThreadPoolConfig {
    public static ThreadPoolExecutor createThreadPool() {
        int coreSize = Runtime.getRuntime().availableProcessors() + 1;
        int maxSize = coreSize * 2;
        
        return new ThreadPoolExecutor(
            coreSize,
            maxSize,
            60L, TimeUnit.SECONDS,
            new LinkedBlockingQueue<>(1000),
            new ThreadFactory() {
                private final AtomicInteger threadNumber = new AtomicInteger(1);
                @Override
                public Thread newThread(Runnable r) {
                    Thread t = new Thread(r, "worker-" + threadNumber.getAndIncrement());
                    t.setDaemon(false);
                    return t;
                }
            },
            new ThreadPoolExecutor.CallerRunsPolicy()
        );
    }
}
```

### 1.6.5 Fork/Join 框架

```java
public class RecursiveTaskExample extends RecursiveTask<Long> {
    private static final int THRESHOLD = 10000;
    private final long start;
    private final long end;
    
    @Override
    protected Long compute() {
        long length = end - start;
        if (length <= THRESHOLD) {
            // 直接计算
            long sum = 0;
            for (long i = start; i <= end; i++) sum += i;
            return sum;
        } else {
            // 分裂为两个子任务
            long middle = (start + end) / 2;
            RecursiveTaskExample left = new RecursiveTaskExample(start, middle);
            RecursiveTaskExample right = new RecursiveTaskExample(middle + 1, end);
            invokeAll(left, right);
            return left.join() + right.join();
        }
    }
}
```

---

> [← 返回目录](SUMMARY.md) | [下一章：JVM 深入 →](02-JVM深入.md)